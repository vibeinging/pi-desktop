# 安全说明

## 支持范围

YiW 当前是开发者预览版，尚未完成生产安全审计。请不要把它直接暴露到公网，也不要把唯一副本的重要数据只保存在预览版本中。

## 报告安全问题

请不要为未修复漏洞创建公开 Issue。请使用 [GitHub 私密漏洞报告](https://github.com/vibeinging/yiWork/security/advisories/new)。

报告请包含受影响版本、最小复现步骤、可访问的数据或可执行的操作，以及是否需要模型输出、网页、MCP、Skill、小程序或本地文件参与。

## 主要边界

- 模型文本、Markdown、网页内容、MCP 返回、Skill、小程序包和导入文件都属于不可信输入。
- Electron preload 是网页与本机能力之间的边界，不应暴露任意命令或通用 IPC。
- 本地工具可能读写文件或执行命令；用户取消后，后端任务也必须停止。
- 小程序安装前必须检查权限、接口范围、版本和升级路径。
- 模型密钥、MCP 凭据、IM 凭据、数据库和 Trace 都属于敏感数据，不应进入仓库或公开日志。

## 发布前最低要求

- 扫描密钥、环境文件、业务数据和无许可二进制。
- 清洗模型生成的 Markdown 与 HTML，并限制窗口跳转。
- 验证权限拒绝、取消、回滚和升级失败路径。
- 在干净环境运行 `npm run setup` 和 `npm run check`。
- 审核所有随源码或安装包分发的第三方许可证。
