import styles from './DividerSectionProperty.module.scss'

// 分隔线 section 无额外属性,占位提示组件
export default function DividerSectionProperty() {
  return <div className={styles.dividerHint}>分隔线 section 无额外属性。</div>
}
